name: build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: build-${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows
            os: windows-latest
            artifact: miru-windows-x64.zip
            upload_name: miru
            name: Windows x64
            asset: build/windows/x64/runner/Release
            package: ''
            upload_path: miru-windows-x64.zip
            zip: false
          - platform: android
            os: ubuntu-latest
            artifact: android
            upload_name: android-apks
            name: Android (3 ABIs)
            asset: build/app/outputs/flutter-apk
            package: ''
            upload_path: miru-android-*.apk
            zip: false
          - platform: macos
            os: macos-latest
            artifact: miru-macos.zip
            upload_name: miru
            name: macOS (universal)
            asset: build/macos/Build/Products/Release
            package: miru.app
            upload_path: miru-macos.zip
            zip: true
          - platform: linux
            os: ubuntu-latest
            artifact: miru-linux-x64.tar.gz
            upload_name: miru
            name: Linux x64
            asset: build/linux/x64/release/bundle
            package: ''
            upload_path: miru-linux-x64.tar.gz
            zip: false
          - platform: ios
            os: macos-latest
            artifact: miru-ios.zip
            upload_name: miru
            name: iOS (no-codesign)
            asset: build/ios/iphoneos
            package: Runner.app
            upload_path: miru-ios.zip
            zip: true

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Enable desktop
        run: flutter config --enable-windows-desktop --enable-macos-desktop --enable-linux-desktop

      - name: Install Linux dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ninja-build libgtk-3-dev libmpv-dev mpv

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows
        if: matrix.platform == 'windows'
        run: flutter build windows --release

      - name: Build Android
        if: matrix.platform == 'android'
        run: flutter build apk --release --split-per-abi

      - name: Build macOS
        if: matrix.platform == 'macos'
        run: flutter build macos --release

      - name: Build Linux
        if: matrix.platform == 'linux'
        run: flutter build linux --release

      - name: Build iOS (no codesign)
        if: matrix.platform == 'ios'
        run: flutter build ios --release --no-codesign

      - name: Package Windows
        if: matrix.platform == 'windows'
        shell: pwsh
        run: Compress-Archive -Path '${{ matrix.asset }}' -DestinationPath '${{ matrix.artifact }}' -Force

      - name: Package Android
        if: matrix.platform == 'android'
        shell: bash
        run: |
          shopt -s nullglob
          for f in "${{ matrix.asset }}"/app-*-release.apk; do
            abi=$(basename "$f" | sed -E 's/app-(.+-)release\.apk/\1/')
            abi=${abi%-}
            cp "$f" "miru-android-${abi}.apk"
            echo "==> miru-android-${abi}.apk"
          done
          ls -la miru-android-*.apk

      - name: Package macOS
        if: matrix.package != ''
        shell: bash
        run: (cd '${{ matrix.asset }}' && zip -r -q "$GITHUB_WORKSPACE/${{ matrix.artifact }}" '${{ matrix.package }}')

      - name: Package Linux
        if: matrix.platform == 'linux'
        shell: bash
        run: tar -C '${{ matrix.asset }}' -czf "$GITHUB_WORKSPACE/${{ matrix.artifact }}" .

      - name: Package iOS
        if: matrix.platform == 'ios'
        shell: bash
        run: (cd '${{ matrix.asset }}' && zip -r -q "$GITHUB_WORKSPACE/${{ matrix.artifact }}" '${{ matrix.package }}')

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.upload_name }}
          path: ${{ matrix.upload_path }}
          if-no-files-found: error

  release:
    name: release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate release notes
        shell: bash
        run: |
          TAG="${GITHUB_REF_NAME}"
          PREV_TAG=$(git describe --tags --abbrev=0 "${TAG}^" 2>/dev/null || true)
          {
            echo "## 更新内容"
            echo ""
            if [ -z "${PREV_TAG}" ]; then
              echo "首次发布，以下为全部提交："
              echo ""
              git log --no-merges --pretty=format:"- %s (%h)" "${TAG}"
              echo ""
            else
              git log --no-merges --pretty=format:"- %s (%h)" "${PREV_TAG}..${TAG}"
              echo ""
            fi
          } > RELEASE_NOTES.md
          echo "release-notes-file=RELEASE_NOTES.md" >> "${GITHUB_OUTPUT}"
        id: notes

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*/*
          tag_name: ${{ github.ref_name }}
          name: miru ${{ github.ref_name }}
          body_path: ${{ steps.notes.outputs.release-notes-file }}
          draft: false